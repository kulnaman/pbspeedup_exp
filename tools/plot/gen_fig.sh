#!/bin/bash -xe
#assuming all processed benchmarks in directory benchmark_data in format {benchmark_name}-{cpu/gpu}
for all benchmark in ./benchmark_data/*.csv; do
	for all figure in ./fig_script/*.py; do
		bechmark_name=${file%-*}
		if []
		then
		
	